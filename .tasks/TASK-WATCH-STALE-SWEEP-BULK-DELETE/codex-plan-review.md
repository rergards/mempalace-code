verdict: NEEDS_CHANGES

gaps:
  - severity: high
    claim: "Acceptance criteria AC-3 and AC-5 have no corresponding regression_plan.checks row linked via acceptance_ids. The standard-plan contract gate requires every acceptance criterion to be covered by a regression check."
    evidence: "docs/plans/WATCH-STALE-SWEEP-BULK-DELETE.md:128-136 — regression_plan.checks only contains REG-1 (acceptance_ids: [AC-1]) and REG-2 (acceptance_ids: [AC-2, AC-4]). AC-3 (no-op delete for empty/no-match) and AC-5 (changed-file/full-rebuild stay separate) are not referenced by any checks row."
    suggested_fix: "Add a regression_plan.checks row referencing AC-3 (e.g. REG-3 running tests/test_storage.py::TestDeleteBySourceFiles::test_empty_or_nonmatching_source_files_do_not_delete to prove no no-op Lance version is created) and a row referencing AC-5 (e.g. REG-4 running tests/test_miner.py::test_incremental_detects_content_change and ::test_incremental_full_flag_forces_rebuild to prove modified-file/full-rebuild deletion stays intact). Both are already authored as verification rows, so reusing them as runnable regression commands is straightforward."
  - severity: medium
    claim: "The plan optimizes the common case to a single Lance version but provides no test or mitigation at the scale that triggered the incident (thousands of stale files). A single `source_file IN (...) AND wing = ...` predicate built from thousands of single-quoted absolute-path literals is untested for acceptance by Lance/DataFusion; if it is rejected or pathologically slow, the fix does not resolve the reported scenario."
    evidence: "Task description reports 'Lance versions climbed into the thousands' (one version per stale file → thousands of stale paths). Plan RISK-4 (docs/plans/WATCH-STALE-SWEEP-BULK-DELETE.md:102-103) acknowledges long predicates but defers chunking; no acceptance criterion or verification exercises a large stale set, and storage tests (AC-2..AC-4) only cover small fixtures."
    suggested_fix: "Either (a) implement bounded chunking now (e.g. delete in batches of N source_files → a small constant number of versions instead of one-per-file), or (b) add a storage test that bulk-deletes a few thousand source_files in one wing to prove the single predicate is accepted and produces the expected count, or (c) explicitly state the verification boundary (single-predicate path tested only at small N; large-N behavior unverified) so reviewers do not assume the incident scale is covered."
