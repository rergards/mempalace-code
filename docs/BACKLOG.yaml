# ============================================================
# BACKLOG.yaml — Machine-readable project backlog
# Format: sections + items (DH2-style)
# Parse:  backlog list --status open --file docs/BACKLOG.yaml
# ============================================================
#
# project: mempalace-code

sections:
  - id: immediate
    order: 1
    title: "IMMEDIATE — Foundation & Project Setup"
    description: "Fork identity, CI, docs. Must be done before any feature work."
  - id: pre_release
    order: 2
    title: "PRE-RELEASE — v1.0 Blockers (Autopilot Pass)"
    description: "P0 and P1 items that must land before a public v1.0. All items here are autopilot-ready — owner decisions already made. Batch in one large autopilot pass. Items needing owner judgment live in other sections."
  - id: coding_first
    order: 3
    title: "CODING-FIRST — Smart Code Mining"
    description: "Language-aware chunking, structural boundaries, code-optimized search. The core differentiator."
  - id: storage_reliability
    order: 4
    title: "STORAGE & RELIABILITY"
    description: "LanceDB hardening, migration tooling, backup/restore."
  - id: mcp_developer_tools
    order: 5
    title: "MCP — Developer-Focused Tools"
    description: "New and improved MCP tools for coding workflows."
  - id: cleanup
    order: 6
    title: "CLEANUP — De-emphasize Dialog-First Features"
    description: "Refactor or gate conversation-centric modules. Keep functional but not default."
  - id: quality
    order: 7
    title: "QUALITY — Tests, Benchmarks, CI"
    description: "Test coverage, code-mining benchmarks, CI hardening."
  - id: autopilot_demo
    order: 8
    title: "AUTOPILOT DEMO — Code Quality Showcase"
    description: "Measurable, public-safe improvements that demonstrate Autopilot can raise code quality without changing product scope."
  - id: future
    order: 9
    title: "FUTURE — Post-v1 Ideas"
    description: "Ideas for after the first public release."
# ============================================================
# OPEN ITEMS
# ============================================================
items:
  - key: REL-ARM-ONNX-PCIBUS-DIAGNOSTIC-CLASSIFIER
    summary: "Classify the bounded ONNX PCI warning on hosted ARM"
    type: bug
    status: open
    priority: highest
    size: S
    section_id: quality
    assignee: bot
    labels: [release-blocker, linux-arm, release-gate, rule-zero]
    description: "## Symptom\n\nExact rc6 run 33456968750 job 99698892826 passed cache provisioning, every installer contour, and installed_golden_fetch_model, then failed later in the golden suite. A credential-free exact-wheel reproduction on the existing Ubuntu 24.04 ARM64 machine identified installed_golden_rollback_no_candidate: the ARM diagnostic classifier calls the PCI-warning regex with stderr=None when a successful command intentionally merges stderr into stdout.\n\n## Steps to reproduce\n\nRun python scripts/release_readiness_gate.py --installed-golden-wheel \"$WHEEL\" --json on Linux ARM64 with the validated MiniLM cache. The rollback scenario reaches its first successful merged-stream command and the classifier raises TypeError: expected string or bytes-like object, got NoneType.\n\n## Expected vs actual\n\nExpected: merged stderr represented by None remains unchanged unless the complete known ARM warning occupies stderr. Actual: the PCI warning branch calls fullmatch(None) and aborts the gate despite successful application behavior.\n\n## Root cause hypothesis\n\nConfirmed locally against the exact rc6 tree and wheel. The rc6 PCI-warning extension preserved exact matching but omitted the standard subprocess invariant that stderr is None when stderr=STDOUT. The earlier cpuid-only equality comparison tolerated None.\n\n## Scope\n\nExtend only _classify_installed_golden_diagnostic and its existing tests. Make the exact PCI-warning match null-safe while preserving the current successful Linux ARM64, exact-single-warning conditions and all fail-closed cases. Add the smallest regression that passes a successful ARM result with stderr=None through the classifier. Regenerate existing scorecard artifacts only if required. Do not change fetch-model, dependencies, cache policy, workflows, runtime logging, warning shapes, or add a helper or gate.\n\n## Acceptance criteria\n\n- A successful Linux ARM64 result with stderr=None is returned unchanged without exception.\n- Exact cpuid_info and GetPciBusId classification remains unchanged.\n- Non-ARM, non-Linux, nonzero, altered, duplicate, and additional stderr remain visible.\n- Focused readiness tests, full non-network suite, scorecard, public safety, formatting, and diff checks pass.\n- A fresh exact-SHA hosted ARM installed-application job completes the rollback row and the entire golden suite; final aggregate closure remains with REL-LINUX-ARM-INSTALLER-HANDSHAKE.\n\nRecovery: rerun python scripts/release_readiness_gate.py --installed-golden-wheel \"$WHEEL\" --json."
  - key: REL-LINUX-X64-DIARY-SIGSEGV
    summary: "Diagnose and fix exact-wheel Linux x64 diary signal 11"
    type: bug
    status: open
    priority: highest
    size: S
    section_id: quality
    assignee: bot
    labels: [release-blocker, linux-x64, installed-golden, rule-zero]
    description: "## Symptom\n\nExact rc6 run 33456968750 job 99698892809 passed the pinned MiniLM compatibility check and all supported installer contours, then failed the full installed golden step. A credential-free amd64 Python 3.11 reproduction of the exact rc6 tree and wheel reaches installed_golden_path_contracts; the first diary write exits -11 with empty stdout and stderr. The hosted step-level match is confirmed; the exact hosted row remains an inference because GitHub does not expose public log bodies anonymously.\n\n## Steps to reproduce\n\nBuild the rc6 wheel in a clean Linux amd64 Python 3.11 environment with the validated read-only MiniLM cache, then run python scripts/release_readiness_gate.py --installed-golden-wheel \"$WHEEL\" --json. Trace the installed console invocation for --palace <palace> diary write: it returns signal 11 before its acknowledgement.\n\n## Expected vs actual\n\nExpected: exact-wheel diary write stores one entry, emits the bounded acknowledgement, and the golden suite continues. Actual: the installed process terminates with signal 11 and no diagnostic in the local amd64 reproduction; the exact hosted x64 job fails the same golden step after compatibility and installers pass.\n\n## Root cause hypothesis\n\nUnknown. The compatibility benchmark succeeds, so model-vector compatibility alone is not the failing predicate. The crash occurs on the first write/embedding path and predates the rc6 Gitleaks-only repair: rc5 installed-application already failed, while rc2 predates this job. Evidence must distinguish native hosted behavior from amd64 emulation before changing runtime code or dependencies.\n\n## Scope\n\nUse the exact wheel and existing path-contract/golden owners. First run the direct diary command with bounded crash diagnostics and compare the owning write/embedding path with the last candidate before this gate existed. Apply the smallest existing-owner correction proved by that evidence. Do not change embedding thresholds, model, cache policy, release topology, or dependency bounds without a decisive native-Linux predicate. Do not add a second gate or diagnostic framework.\n\n## Acceptance criteria\n\n- The exact failing native-Linux predicate and owning code path are recorded without credentials or private paths.\n- A focused regression fails before the fix and passes after it.\n- Exact-wheel diary write passes directly on Linux x64 with one success and one important refusal/retry path, with bounded output and no network.\n- The full exact-wheel golden suite passes on Linux x64 and macOS; focused tests, full non-network suite, scorecard, public safety, formatting, and diff checks pass.\n- A fresh exact-SHA hosted x64 installed-application job and release-required pass.\n\nRecovery: rerun python scripts/release_readiness_gate.py --installed-golden-wheel \"$WHEEL\" --json in the same clean Linux x64 environment."
  - key: REL-LINUX-ARM-INSTALLER-HANDSHAKE
    summary: "Make hosted ARM installer smoke establish its owned identity"
    type: bug
    status: open
    priority: highest
    size: S
    section_id: quality
    assignee: bot
    labels: [release-blocker, linux-arm, installer, rule-zero]
    description: "## Symptom\n\nPublic candidate run 33328559281 failed before installer execution because the disposable child wrote its temporary identity with a mode that did not satisfy the immediate 0600 postcondition. The later missing-identity and cleanup messages were secondary.\n\n## Steps to reproduce\n\nRun the installed-application job on ubuntu-24.04-arm for the exact candidate wheel and observe the embedded launcher fail at test \"$(stat -c %a \"$2\")\" = 600 before atomic identity publication.\n\n## Expected vs actual\n\nExpected: the temporary identity passes its existing 0600 check, publishes atomically, and the job reaches all-installers and installed-golden execution. Actual: the child exits at the mode check and the outer job reports missing identity.\n\n## Root cause hypothesis\n\nConfirmed by hosted x64 and ARM output: the embedded launcher relies on umask alone, while the exact post-write mode observed by the hosted shell does not equal 600. Existing native ARM evidence completed every installer, the 26-row golden suite, systemd lifecycle, and cleanup, so product installers and the downstream smoke are not the first failing owner.\n\n## Scope\n\nExtend the existing embedded launcher in .github/workflows/ci.yml with an explicit chmod 0600 -- \"$2\" immediately after the identity write and before the existing regular-file, owner, mode, and atomic mv checks. Extend the smallest existing workflow admission assertion for this ordering. Inspect prior attempt b660e2bd as evidence only; do not merge it wholesale. Do not add a deadline protocol, diagnostic receipt, new cleanup behavior, blind sleep, helper, or second handshake owner.\n\n## Acceptance criteria\n\n- The temporary identity is explicitly mode 0600 after write and before existing validation and same-directory atomic publication.\n- Existing identity, PGID, interpreter, signal, cleanup, exact-wheel, installer, and golden behavior is otherwise byte-for-byte unchanged.\n- The smallest existing workflow admission test proves write then chmod then mode check then mv; focused workflow tests, Actionlint, diff checks, and public safety pass.\n- One exact-SHA public candidate run reaches and completes the hosted Ubuntu 24.04 ARM64 installed-application job and release-required. Record that receipt when closing this task; do not create a second evidence task."
  - key: SEC-GITLEAKS-HISTORY-GATE
    summary: "Restore compact Gitleaks assurance guarantees"
    type: bug
    status: open
    priority: highest
    size: S
    section_id: quality
    assignee: bot
    labels: [release-blocker, security, gitleaks, rule-zero]
    description: "## Symptom\n\nThe native changed-range and full-history scans remained intact after simplification, while machine-checked suppression rationale and the real-binary detector/redaction fixture were removed. That left two current release-assurance predicates unproved.\n\n## Steps to reproduce\n\nAt task opening, python scripts/gitleaks_scan.py validate-baseline and python scripts/gitleaks_scan.py fixture-smoke were absent, and an unreviewed exact-looking .gitleaksignore line passed the only generic uniqueness check. The compact local implementation now makes both commands executable; temporary-root tests reproduce malformed suppression and incomplete SARIF failures without changing repository policy.\n\n## Current status\n\nThe compact same-owner implementation is present locally and this release blocker remains open for parent qualification. scripts/gitleaks_scan.py now exposes validate-baseline and fixture-smoke; every scan validates suppression policy before invoking Gitleaks. The installed real binary has locally proved all five required classes with redacted disposable SARIF.\n\n## Scope\n\nKeep the existing native launcher, .gitleaksignore, tests, canonical gate inventory, quality scorecard, verify instructions, and the Gitleaks jobs in ci.yml, gitleaks-history.yml, and publish.yml as the only owners. Each exact fingerprint has strict adjacent JSON metadata with owner, rationale, and review_condition or nonexpired expiry. No current fingerprint value changes. fixture-smoke runs after the pinned installer and before the repository scan in all three workflows. validate-baseline stays an explicit manual command and is not duplicated as a workflow step. Imported Gitleaks config sources are forbidden.\n\n## Acceptance criteria\n\n- Orphan, incomplete, duplicate, expired, malformed, globbed, regex/broad, rule-disabled, imported, allowlisted, or ungoverned suppression input fails before a repository scan.\n- The parser and tests support any number of future reviewed exact fingerprints without literal fingerprint or count assertions.\n- Gitleaks exit 1 is fixture success only when bounded redacted SARIF proves PyPI token, GitHub token, AWS access key, private key, and mempalace-high-entropy-assignment filename/rule pairs.\n- Fixture source, worktree, and report live in a TemporaryDirectory outside the checkout and Gitleaks upload paths, are removed on success or failure, and never place complete generated values in retained SARIF, logs, or tracked source.\n- Exact ref validation, shallow-history rejection, changed-range and full-history behavior, pinned installation, redaction, SARIF uploads, bounded output, canonical command ownership, and all release gates remain intact.\n- Focused tests, real validate-baseline, real fixture-smoke, safe reachable changed-range, Actionlint, focused Ruff, gate inventory, scorecard freshness, working-tree Gitleaks, and public safety pass locally. Parent qualification owns full history, the full non-network suite, and hosted workflow evidence.\n\n## Rule Zero\n\nExtending the existing scanner, ignore, workflow, gate-inventory, and scorecard owners has the lowest total complexity and removal cost. Restoring the old wrapper, security/gitleaks-baseline.yml, PyYAML, release_direct_application_gate.py, workflow_security_gate.py, or deleted release machinery would recreate parallel owners and is forbidden. Cheapest falsifiers are an unreviewed temporary fingerprint rejected before _run, an imported config or allowlist rejected before scan, a missing or unredacted fixture SARIF class rejected after exit 1, and canonical installer-before-fixture-before-scan assertions.\n\n## Remaining closure evidence\n\nRun parent qualification and hosted workflows against the exact candidate SHA. Pinned detector rule-id drift fails closed by design; recover by reviewing the Gitleaks pin and expected filename/rule mapping, then update the mapping only when the maintained detector identity changed. Never weaken detection to make drift pass. Record the local Gitleaks version separately when it differs from tools/gitleaks/go.mod. Keep this item open until that evidence is complete."
