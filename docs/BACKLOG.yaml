# ============================================================
# BACKLOG.yaml — Machine-readable project backlog
# Format: sections + items (DH2-style)
# Parse:  backlog list --status open --file docs/BACKLOG.yaml
# ============================================================
#
# project: mempalace-code

sections:
  - id: immediate
    order: 1
    title: "IMMEDIATE — Foundation & Project Setup"
    description: "Fork identity, CI, docs. Must be done before any feature work."
  - id: pre_release
    order: 2
    title: "PRE-RELEASE — v1.0 Blockers (Autopilot Pass)"
    description: "P0 and P1 items that must land before a public v1.0. All items here are autopilot-ready — owner decisions already made. Batch in one large autopilot pass. Items needing owner judgment live in other sections."
  - id: coding_first
    order: 3
    title: "CODING-FIRST — Smart Code Mining"
    description: "Language-aware chunking, structural boundaries, code-optimized search. The core differentiator."
  - id: storage_reliability
    order: 4
    title: "STORAGE & RELIABILITY"
    description: "LanceDB hardening, migration tooling, backup/restore."
  - id: mcp_developer_tools
    order: 5
    title: "MCP — Developer-Focused Tools"
    description: "New and improved MCP tools for coding workflows."
  - id: cleanup
    order: 6
    title: "CLEANUP — De-emphasize Dialog-First Features"
    description: "Refactor or gate conversation-centric modules. Keep functional but not default."
  - id: quality
    order: 7
    title: "QUALITY — Tests, Benchmarks, CI"
    description: "Test coverage, code-mining benchmarks, CI hardening."
  - id: autopilot_demo
    order: 8
    title: "AUTOPILOT DEMO — Code Quality Showcase"
    description: "Measurable, public-safe improvements that demonstrate Autopilot can raise code quality without changing product scope."
  - id: future
    order: 9
    title: "FUTURE — Post-v1 Ideas"
    description: "Ideas for after the first public release."
# ============================================================
# OPEN ITEMS
# ============================================================
items:
  - key: RELEASE-DIRECT-INSTALLED-APP-GATE
    summary: "Make direct exact-wheel application exercise the installed release gate"
    type: task
    status: open
    priority: highest
    size: L
    section_id: pre_release
    description: "## Goal\n\nRequire the existing release-readiness owner to execute the exact candidate wheel as installed applications and fail closed on any declared public surface without direct evidence.\n\n## Completed evidence\n\n- The installed-golden owner runs 26 ordered direct exact-wheel rows without pytest or test-module execution.\n- Installed argparse discovery reconciles 43 CLI command and mode members to direct candidate-console execution.\n- All five MCP profiles and 29 installed tools have direct semantic JSON-RPC evidence, palace-local post-state, and protected-root isolation.\n- Canonical venv, bootstrap-venv, pipx, and uv-tool contours install the exact wheel and prove metadata, import provenance, CLI version, alias, Agent Plugin, ordinary LanceDB runtime, recovery refusals, network denial, and disposable cleanup.\n\n## Scope\n\nReuse scripts/release_readiness_gate.py, scripts/release_install_metadata_smoke.py, their provenance/isolation/subprocess/result owners, and the existing hosted release workflow. Complete one independently testable residual at a time. Do not add a second runner, gate row, catalog, service, durable state owner, dependency unrelated to the declared extras, AI-client call, credential access, publication, tag, push, or remote mutation.\n\n## Acceptance criteria\n\n- AC-1: Install and directly exercise each declared optional extra and the public installed import surface through the existing installer-smoke owner. The smallest valid behavior or intended refusal must prove each extra; import success alone is insufficient where a public behavior exists.\n- AC-2: On a supported Linux user session, run the exact installed candidate through confirmed systemd-user scheduler install, status, and removal plus safe update-apply behavior. Record semantic post-state and cleanup. Unavailable systemd-user support must emit blocking UNRUN.\n\n## Continuation\n\nOptional extras and public installed imports are next. The Linux systemd-user contour follows after that residual is independently qualified and archived."
    assignee: locked
  - key: RELEASE-DIRECT-INSTALLED-LINUX-SYSTEMD
    summary: "Qualify exact-wheel Linux systemd-user update lifecycle"
    type: task
    status: open
    priority: highest
    size: M
    section_id: pre_release
    assignee: bot
    description: "## Scope\n\nExtend the existing exact-wheel installer smoke, updater, and Ubuntu installed-application job to qualify the supported Linux systemd-user scheduler and safe update lifecycle from the exact candidate. The installer smoke remains the lifecycle evidence owner and blocks the existing release-required aggregate. Use one disposable Linux user whose systemd-user manager and HOME share an ownership boundary. Refuse before mutation for foreign, ambiguous, partial, symlinked, non-regular, escaped, or wrong-owner unit paths. Reuse current candidate provenance, credential-free environments, update and scheduler commands, semantic post-state, cleanup, sanitization, subprocess bounds, and aggregate job result. Do not add a second runner, gate row, service manager, updater, package manager, durable state owner, AI-client call, credential access, publication, tag, or unrelated remote mutation.\n\n## Acceptance criteria\n\n- AC-1: On supported Linux with functional systemd --user and matching HOME, install the exact candidate and directly exercise scheduler install, status, repeat, and removal through the installed executable. Prove candidate provenance, fragment path and content, enabled and active semantics, and complete cleanup.\n- AC-2: Existing canonical unit paths with non-matching content, partial ownership, symlinks, non-regular files, foreign uid, path escape, or an incongruent manager and HOME boundary refuse before write, enable, disable, delete, or daemon reload. Matching owned files support bounded idempotent install and removal.\n- AC-3: Directly exercise update status, confirmation refusal, and confirmed safe update apply behavior. Require bounded noninteractive behavior, exact semantic result, and no credential or AI-client access. A deterministic unpublished-candidate or blocked-network result is valid only when it proves zero package and user-unit mutation.\n- AC-4: Preserve unsupported-platform and unavailable-systemd refusals. Missing supported Linux evidence remains blocking UNRUN with one recovery command; mocks, source-only tests, and non-Linux refusal evidence cannot pass it.\n- AC-5: Keep the existing release readiness gate and installed-golden row set unchanged. Require focused false-PASS and ownership regressions, affected and full suites, direct exact-installed Linux evidence in the current Ubuntu job, static workflow and public-safety gates, and independent correctness, security, and Rule Zero reviews.\n\n## Current evidence\n\nThe exact candidate wheel SHA-256 is 48eba064520a75afbfec266caa197c9cd3f50445b86923caa1bbfe90977e29cf. It passed all 26 direct installed-golden rows offline. The full source suite passed 4,234 tests with 4 skipped and 2 deselected; focused, artifact, public-safety, secret, static, workflow, and independent reviews passed. Non-Linux release-readiness passed every supported installer surface and correctly retained the Linux lifecycle result as UNRUN. A bounded local Linux attempt exhausted its available memory before complete lifecycle evidence, so it cannot establish PASS or product FAIL.\n\n## Next action\n\nRun the unchanged public installed-application job for the exact pushed candidate commit. Require one terminal release-required PASS with all four installers, linux_systemd_update_lifecycle=pass, and complete disposable-user cleanup. A hosted PASS closes this residual. A hosted out-of-memory or nonzero lifecycle result blocks closure and requires a separate architecture or operations decision. Recovery: delete only the remote candidate branch and retain blocking UNRUN; do not create a tag or publication.\n\n## Hosted failure — 2026-08-27\n\nExact candidate SHA `52ca56703217ffa938a543f250dcdd2bf7470ed6` reached the cache-miss bootstrap and exact-wheel provisioning in public run `33098437503`, then failed before the smoke script started: the disposable user could not open `scripts/release_install_metadata_smoke.py` through the private GitHub workspace path. Repair only the existing workflow staging boundary: materialize the exact read-only smoke script and wheel in one disposable user-readable contour, prove regular-file identity and readability before invocation, preserve credential-free `env -i`, and remove the contour in the existing cleanup path. Do not chmod the checkout, weaken exact-wheel provenance, add a job, or claim lifecycle PASS until the terminal aggregate and cleanup pass. Recovery: remove only the staged contour and disposable user, then rerun the same installed-application job.\n\n## Current attempt boundary\n\nChange only `.github/workflows/ci.yml`, the existing workflow-admission regression in `tests/test_release_workflow_admission.py`, and deterministic `docs/quality/scorecard.{md,json}` output required by the existing scorecard owner. Stage the exact script and wheel under an atomically created `mktemp -d /var/tmp/mempalace-installed-application.XXXXXX` contour because paths beneath `/home/runner` may remain untraversable to the disposable user. Preserve the wheel's canonical basename; renaming it to `candidate.whl` or another non-wheel filename is invalid. Prove source/staged byte identity, regular non-symlink files, root ownership, read-only modes, disposable-user readability, exact-file cleanup without recursive deletion, and unchanged credential-free invocation. Do not change `scripts/release_install_metadata_smoke.py`, product code, installer orchestration, release docs, scorecard logic, job topology, or manager coverage. The prior local OOM is not evidence for an orchestration change; only a hosted OOM after the staging repair may reopen that decision."
    links:
      - type: subtask-of
        key: RELEASE-DIRECT-INSTALLED-APP-GATE
  - key: REL-DEPENDENCY-REPORT-CURRENT-HASH
    summary: "Regenerate the v1.13.5 dependency report for current hashes"
    type: task
    status: open
    priority: high
    size: S
    section_id: pre_release
    description: "## Scope\n\nRegenerate docs/dependency-upgrade-reports/v1.13.5-release.json with the existing scripts/dependency_upgrade_gate.py audit path and the established REL-DEPENDENCY-AUDIT-1-13-5 contract. Keep pyproject.toml, uv.lock, dependency bounds, gate code, CI behavior, and report schema unchanged. Use one transient manifest covering runtime, dev, chroma, and chroma-migration with exact current locked targets. Commit only the sanitized successful report; retain no manifest, resolver output, cache path, hostname, credential, token, or temporary artifact.\n\n## Acceptance criteria\n\n- verify-report accepts the report against the current pyproject.toml and uv.lock hashes.\n- resolver_audits contains successful default, dev, chroma, and chroma-migration rows in the established order.\n- ci-check passes against publish/main while the existing stale-report regression still fails closed.\n- public-safety and Gitleaks find no private or secret material.\n- Any advisory or resolver failure blocks replacement of the current report and returns one recovery command."
  - key: REL-SCHEDULE-SNIPPET-LINUX-MARKER
    summary: "Accept Linux cron evidence in installed schedule-snippet qualification"
    type: bug
    status: open
    priority: highest
    size: XS
    section_id: pre_release
    assignee: bot
    description: "## Symptom\n\nThe exact candidate hosted suite fails `test_installed_schedule_snippets_bind_to_invoked_launcher` on every supported Linux/Python job even though the invoked source console exits zero and renders the expected deterministic cron re-render and install guidance.\n\n## Steps to reproduce\n\nRun `python -m pytest tests/test_cli_golden_scenarios.py::test_installed_schedule_snippets_bind_to_invoked_launcher -q` on Linux. The release qualifier requires macOS `com.mempalace.*.plist` names in stderr for both platforms and rejects the valid cron output.\n\n## Scope\n\nCorrect only the existing installed schedule-snippet qualification in `scripts/release_readiness_gate.py` and its focused tests. Reuse the existing backup/watch renderers, invoked-launcher binding, quoting, determinism, mutation boundary, install refusal, sanitization, and recovery command. Do not add a scheduler renderer, platform abstraction, release row, workflow job, or user-facing behavior.\n\n## Expected vs actual\n\nExpected: Linux qualification requires the existing cron-specific install marker while Darwin qualification requires the existing plist name. Actual: both platforms require the Darwin plist name.\n\n## Root cause hypothesis\n\nThe qualifier stores one unconditional plist marker per case instead of selecting the existing platform-specific scheduler evidence.\n\n## Acceptance criteria\n\n- The focused source-console scenario passes on Linux only when backup and watch output retain the exact invoked launcher, target quoting, deterministic output, cron install guidance, clean stderr/stdout bounds, refusal behavior, unchanged state, and cleanup.\n- Darwin still requires the exact `com.mempalace.backup.plist` and `com.mempalace.watch.plist` evidence.\n- Removing either platform marker or substituting the ambient launcher makes the focused test fail closed.\n- Focused tests, Ruff, scorecard generation/check, public safety, and the hosted Python matrix pass. Recovery: revert only the platform-marker qualification hunk."
