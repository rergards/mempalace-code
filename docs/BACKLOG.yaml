# ============================================================
# BACKLOG.yaml — Machine-readable project backlog
# Format: sections + items (DH2-style)
# Parse:  backlog list --status open --file docs/BACKLOG.yaml
# ============================================================
#
# project: mempalace-code

sections:
  - id: immediate
    order: 1
    title: "IMMEDIATE — Foundation & Project Setup"
    description: "Fork identity, CI, docs. Must be done before any feature work."
  - id: pre_release
    order: 2
    title: "PRE-RELEASE — v1.0 Blockers (Autopilot Pass)"
    description: "P0 and P1 items that must land before a public v1.0. All items here are autopilot-ready — owner decisions already made. Batch in one large autopilot pass. Items needing owner judgment live in other sections."
  - id: coding_first
    order: 3
    title: "CODING-FIRST — Smart Code Mining"
    description: "Language-aware chunking, structural boundaries, code-optimized search. The core differentiator."
  - id: storage_reliability
    order: 4
    title: "STORAGE & RELIABILITY"
    description: "LanceDB hardening, migration tooling, backup/restore."
  - id: mcp_developer_tools
    order: 5
    title: "MCP — Developer-Focused Tools"
    description: "New and improved MCP tools for coding workflows."
  - id: cleanup
    order: 6
    title: "CLEANUP — De-emphasize Dialog-First Features"
    description: "Refactor or gate conversation-centric modules. Keep functional but not default."
  - id: quality
    order: 7
    title: "QUALITY — Tests, Benchmarks, CI"
    description: "Test coverage, code-mining benchmarks, CI hardening."
  - id: autopilot_demo
    order: 8
    title: "AUTOPILOT DEMO — Code Quality Showcase"
    description: "Measurable, public-safe improvements that demonstrate Autopilot can raise code quality without changing product scope."
  - id: future
    order: 9
    title: "FUTURE — Post-v1 Ideas"
    description: "Ideas for after the first public release."
# ============================================================
# OPEN ITEMS
# ============================================================
items:
  - key: REL-MINILM-PUBLIC-CORPUS-PIN
    summary: "Move MiniLM compatibility corpus to a publicly reproducible commit"
    type: bug
    status: open
    priority: highest
    size: S
    section_id: quality
    description: "## Symptom\n\nHosted x64 installed-application job 99121176043 stops before embedding with `pinned MiniLM compatibility commit is unavailable` for 3ad086bfd15bab032e86bef3e9deec207c13c17b.\n\n## Steps to reproduce\n\nFrom a fresh clone of `rergards/mempalace-code`, build/install the exact wheel and run `python benchmarks/code_retrieval_bench.py --check-minilm-runtime-compatibility`.\n\n## Scope\n\nExtend the existing benchmark contract in `benchmarks/retrieval_quality_facts.json` and its owning runner/tests. Select a commit already reachable from the public repository and remeasure the compatibility facts. Do not publish the old private/local commit, weaken missing-history refusal, substitute the current SHA without measurement, or claim the 469-chunk headline was rerun.\n\n## Expected vs actual\n\nExpected: the committed reproduction command runs from a public clean clone. Actual: the pin exists only on a local/private feature branch and is not an ancestor of the public candidate.\n\n## Root cause hypothesis\n\nThe historical benchmark commit was retained across the public-history squash without checking public reachability.\n\n## Acceptance criteria\n\n- AC-1: The pin resolves from a fresh public clone and no private commit or tree is published.\n- AC-2: The exact installed wheel runs the compatibility benchmark offline against the seeded MiniLM cache.\n- AC-3: Query and chunk counts plus R@5 and R@10 are measured and committed truthfully; focused tests and x64 installed-application pass."
  - key: REL-LINUX-ARM-INSTALLER-HANDSHAKE
    summary: "Repair the hosted ARM installer identity handshake"
    type: bug
    status: open
    priority: highest
    size: S
    section_id: quality
    description: "## Symptom\n\nHosted ARM64 installed-application job 99121175976 in run 33260376052 fails before the application starts: the disposable-user launcher produces no `.mempalace-release-smoke.identity`, then cleanup reports secondary timer/mail-spool noise. Earlier runs 33202518999 and 33195680169 show the same boundary.\n\n## Steps to reproduce\n\nRun the existing `.github/workflows/ci.yml` disposable-user/systemd wrapper on native Ubuntu ARM64/Python 3.12 with the exact candidate wheel. Preserve bounded launcher stderr and exit status before reading the identity file.\n\n## Scope\n\nRepair the existing workflow launch/handshake and cleanup owner. Do not modify product installer behavior until evidence proves the installed script is reached. Do not add another gate, AI client, authentication, credential access, or raw unbounded log exposure.\n\n## Expected vs actual\n\nExpected: identity is atomically published, the real all-installers aggregate runs, and the exact user/systemd/staging state is cleaned. Actual: launcher exits before identity publication and the current workflow hides the primary cause behind `cat`.\n\n## Root cause hypothesis\n\nDecision-critical unknown inside the sudo/env/setsid/bash pre-handshake wrapper; current observability loses the child failure.\n\n## Acceptance criteria\n\n- AC-1: A first diagnostic reproduction identifies the inner cause before implementation.\n- AC-2: The smallest same-owner repair makes two consecutive native ARM wrapper runs reach the installed aggregate and clean all user, unit, process, and staging state.\n- AC-3: A forced pre-handshake failure reports bounded actionable state before any `cat`, and the hosted ARM job passes."
