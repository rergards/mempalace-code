# ============================================================
# BACKLOG.yaml — Machine-readable project backlog
# Format: sections + items (DH2-style)
# Parse:  backlog list --status open --file docs/BACKLOG.yaml
# ============================================================
#
# project: mempalace-code

sections:
  - id: immediate
    order: 1
    title: "IMMEDIATE — Foundation & Project Setup"
    description: "Fork identity, CI, docs. Must be done before any feature work."
  - id: pre_release
    order: 2
    title: "PRE-RELEASE — v1.0 Blockers (Autopilot Pass)"
    description: "P0 and P1 items that must land before a public v1.0. All items here are autopilot-ready — owner decisions already made. Batch in one large autopilot pass. Items needing owner judgment live in other sections."
  - id: coding_first
    order: 3
    title: "CODING-FIRST — Smart Code Mining"
    description: "Language-aware chunking, structural boundaries, code-optimized search. The core differentiator."
  - id: storage_reliability
    order: 4
    title: "STORAGE & RELIABILITY"
    description: "LanceDB hardening, migration tooling, backup/restore."
  - id: mcp_developer_tools
    order: 5
    title: "MCP — Developer-Focused Tools"
    description: "New and improved MCP tools for coding workflows."
  - id: cleanup
    order: 6
    title: "CLEANUP — De-emphasize Dialog-First Features"
    description: "Refactor or gate conversation-centric modules. Keep functional but not default."
  - id: quality
    order: 7
    title: "QUALITY — Tests, Benchmarks, CI"
    description: "Test coverage, code-mining benchmarks, CI hardening."
  - id: autopilot_demo
    order: 8
    title: "AUTOPILOT DEMO — Code Quality Showcase"
    description: "Measurable, public-safe improvements that demonstrate Autopilot can raise code quality without changing product scope."
  - id: future
    order: 9
    title: "FUTURE — Post-v1 Ideas"
    description: "Ideas for after the first public release."
# ============================================================
# OPEN ITEMS
# ============================================================
items:
  - key: REL-CI-UPSTREAM-INVENTORY-CLEAN-CHECKOUT
    summary: "Make exact upstream inventory pass in a squash-candidate checkout"
    type: bug
    status: open
    priority: highest
    size: S
    section_id: quality
    assignee: bot
    labels: [release-blocker, ci, rule-zero]
    description: "## Symptom\n\nPublic candidate 5cc2da3a3ef7987bf78ec92035959ba27ee1c624 failed Tests run 33328559281 because the static upstream guard could not resolve dfba59b0f3b1c5b57a3d606317b2fd37a4fef6f0..a9f345cc63254eb4dea7abad36963b85c9f8453a from the intentional one-parent public squash history.\n\n## Steps to reproduce\n\nCreate a clean candidate whose sole parent is public main and whose tree equals the reviewed release tree, then run python scripts/upstream_comparison_guard.py --json.\n\n## Scope\n\nReuse scripts/upstream_comparison_guard.py and the existing package/preflight workflow owner. Compare the unmerged 371e22f0 implementation before writing; accept it only if it preserves the intended credential-free/static boundary and lower total complexity. Do not add AI clients, credentials, a second guard, or duplicate the same gate in multiple jobs.\n\nThe static reviewed trust anchor must bind the canonical repository, branch, previous/current full pins, inventory algorithm/version, and the sorted set of full inventory SHAs. A fingerprint of the inventory alone is insufficient because stale or mistaken range metadata could otherwise retain the old inventory and pass.\n\n## Expected vs actual\n\nExpected: the canonical squash release candidate proves the exact 43-commit inventory for the exact documented upstream repository, branch, and range. Actual: git rev-list exits 128 because upstream-only objects are absent.\n\n## Root cause hypothesis\n\nThe guard assumes pinned upstream Git objects exist locally while the canonical public candidate intentionally contains only fork history.\n\n## Acceptance criteria\n\n- A clean one-parent squash-candidate checkout passes the exact inventory and release preflight.\n- The reviewed static trust anchor covers repository, branch, both full endpoint pins, algorithm/version, count, and sorted full inventory SHAs.\n- Substituting repository, branch, either endpoint pin, count, or any inventory SHA while leaving the other fields unchanged fails closed.\n- Duplicate inventory entries fail closed; failure output gives one recovery command.\n- The solution has one gate owner and no credential, token, external AI client, or hidden authenticated dependency.\n- Focused upstream/preflight/workflow tests, the full non-network suite, docs drift, public safety, Ruff, and actionlint pass."
  - key: REL-MINILM-PUBLIC-CORPUS-PIN
    summary: "Make MiniLM compatibility self-contained in public squash candidates"
    type: bug
    status: open
    priority: highest
    size: M
    section_id: quality
    assignee: bot
    labels: [release-blocker, benchmark, rule-zero]
    description: "## Symptom\n\nPublic candidate run 33328559281 x64 installed-application failed before installer/golden execution: pinned commit 3ad086bfd15bab032e86bef3e9deec207c13c17b was unavailable from the public squash history.\n\n## Steps to reproduce\n\nFrom a clean checkout of candidate 5cc2da3a3ef7987bf78ec92035959ba27ee1c624 run python benchmarks/code_retrieval_bench.py --check-minilm-runtime-compatibility.\n\n## Scope\n\nReuse the existing benchmark owner and review the already implemented c296d37f, 79921474, 2d7b03f4 and 87571bdc series before writing. Integrate only the residual self-contained corpus/provenance behavior that remains correct against current HEAD; avoid importing unrelated branch history or adding a new benchmark owner.\n\n## Expected vs actual\n\nExpected: exact-wheel compatibility is reproducible from the published release tree. Actual: it depends on an unreachable historical Git object.\n\n## Root cause hypothesis\n\nThe benchmark treats non-published repository history as release input.\n\n## Acceptance criteria\n\n- A clean public squash-candidate checkout runs the pinned MiniLM compatibility command without fetching an unreachable Git object.\n- Corpus provenance, exact chunk/query counts, measured values, regression thresholds, and current runtime-owner binding fail closed.\n- The committed release shape contains only the minimal public-safe reproducibility fixture and excludes generated scratch output.\n- Focused benchmark, artifact, docs, public-safety, Ruff, and full non-network gates pass."
  - key: REL-DEPENDENCY-REPORT-CURRENT-HASH
    summary: "Refresh the v1.13.5 dependency report after final dependency changes"
    type: task
    status: open
    priority: highest
    size: XS
    section_id: quality
    description: "## Scope\n\nRegenerate the existing v1.13.5 dependency report through scripts/dependency_upgrade_gate.py after the final custom-model/default dependency changes. Current public candidate run 33328559281 reports no document matching pyproject.toml SHA-256 9c9af191fb23aa109d957d92b0f146c257ef69b7146ad385be04c00c988daa30 and uv.lock SHA-256 896e921f44e7e22aafd6e4c0f40f74eabc647456569b0c058405fbadbec34a61. Reuse the existing report owner; do not create a second report format or change the gate.\n\n## Acceptance criteria\n\n- The canonical v1.13.5 report matches current pyproject.toml and uv.lock hashes.\n- Audit evidence covers default, dev, and every remaining optional extra; removed Chroma dependencies remain absent.\n- verify-report and ci-check --base-ref publish/main pass.\n- Public-safety, docs drift, Ruff, and dependency-focused tests pass."
  - key: REL-LINUX-ARM-INSTALLER-HANDSHAKE
    summary: "Make hosted ARM installer smoke establish its owned identity"
    type: bug
    status: open
    priority: highest
    size: M
    section_id: quality
    assignee: bot
    labels: [release-blocker, linux-arm, installer, rule-zero]
    description: "## Symptom\n\nGitHub run 33328559281 job 99302917340 failed before the installed application smoke could be observed: /home/mempalace-ci-33328559281-1/.mempalace-release-smoke.identity was absent, then cleanup reported the missing timer/mail spool.\n\n## Steps to reproduce\n\nRun the installed-application job on ubuntu-24.04-arm for the exact candidate wheel and observe the identity handshake before --all-installers.\n\n## Scope\n\nReuse the existing installed-application workflow and release_install_metadata_smoke.py owner. Inspect the prior REL-LINUX-ARM-INSTALLER-HANDSHAKE attempt branches and native ARM receipt before writing. Fix the earliest proven failing boundary; do not weaken identity/process-group/cleanup checks or add arbitrary sleeps.\n\n## Expected vs actual\n\nExpected: the disposable user atomically publishes its identity, runs all installer and golden contours, and is fully removed. Actual: the owner process exits or races before the identity file is available.\n\n## Root cause hypothesis\n\nThe hosted ARM launcher/identity handshake has a scheduling or executable-access assumption not exercised by the prior native receipt.\n\n## Acceptance criteria\n\n- Hosted Ubuntu 24.04 ARM64 reaches and completes all supported installer and golden rows for the exact wheel.\n- Identity is atomic, owned by the disposable uid, tied to the expected process group and interpreter, and bounded by an explicit deadline.\n- Early child failure reports its sanitized primary diagnostic instead of a secondary missing-file error.\n- Timer, user, process group, stage directory, identity files, and package artifacts are absent after success and failure.\n- Focused workflow/smoke tests, shell/static checks, public safety, and native or hosted ARM evidence pass."
  - key: REL-LINUX-SANITIZER-PATH-BOUNDARY
    summary: "Preserve executable recovery commands while redacting Linux temp paths"
    type: bug
    status: open
    priority: highest
    size: S
    section_id: quality
    assignee: bot
    labels: [release-blocker, linux, drunk-path, rule-zero]
    description: "## Symptom\n\nAll hosted Python test jobs in run 33328559281 failed test_installed_custom_models_enospc_has_one_owned_tmpdir_retry because Linux tempfile root /tmp was substring-replaced inside TMPDIR=\"$HOME/.cache/mempalace/tmp\", producing a non-executable [REDACTED-PATH] recovery command.\n\n## Steps to reproduce\n\nRun TMPDIR=/tmp python -m pytest tests/test_release_readiness_gate.py::test_installed_custom_models_enospc_has_one_owned_tmpdir_retry -q.\n\n## Scope\n\nExtend the existing release_install_metadata_smoke.sanitize owner with path-boundary-aware explicit local-path replacement. Do not add a second sanitizer, a new mode, or a special-case recovery string.\n\n## Expected vs actual\n\nExpected: concrete /tmp/private-build is redacted and the literal $HOME/.cache/mempalace/tmp recovery path remains copy-pasteable exactly once. Actual: both are redacted by raw substring replacement.\n\n## Root cause hypothesis\n\nExplicit local paths are sanitized with str.replace without token/path boundaries.\n\n## Acceptance criteria\n\n- The exact TMPDIR=/tmp reproduction passes.\n- Concrete /tmp and other configured local paths remain redacted at path boundaries, including punctuation and end-of-string cases.\n- Embedded suffixes such as $HOME/.cache/mempalace/tmp remain unchanged.\n- The ENOSPC diagnostic retains one status, one executable recovery command, no private path, and bounded output.\n- Focused sanitizer/readiness/status tests, Ruff, formatting, and full non-network tests pass."
