# ============================================================
# BACKLOG.yaml — Machine-readable project backlog
# Format: sections + items (DH2-style)
# Parse:  backlog list --status open --file docs/BACKLOG.yaml
# ============================================================
#
# project: mempalace-code

sections:
  - id: immediate
    order: 1
    title: "IMMEDIATE — Foundation & Project Setup"
    description: "Fork identity, CI, docs. Must be done before any feature work."
  - id: pre_release
    order: 2
    title: "PRE-RELEASE — v1.0 Blockers (Autopilot Pass)"
    description: "P0 and P1 items that must land before a public v1.0. All items here are autopilot-ready — owner decisions already made. Batch in one large autopilot pass. Items needing owner judgment live in other sections."
  - id: coding_first
    order: 3
    title: "CODING-FIRST — Smart Code Mining"
    description: "Language-aware chunking, structural boundaries, code-optimized search. The core differentiator."
  - id: storage_reliability
    order: 4
    title: "STORAGE & RELIABILITY"
    description: "LanceDB hardening, migration tooling, backup/restore."
  - id: mcp_developer_tools
    order: 5
    title: "MCP — Developer-Focused Tools"
    description: "New and improved MCP tools for coding workflows."
  - id: cleanup
    order: 6
    title: "CLEANUP — De-emphasize Dialog-First Features"
    description: "Refactor or gate conversation-centric modules. Keep functional but not default."
  - id: quality
    order: 7
    title: "QUALITY — Tests, Benchmarks, CI"
    description: "Test coverage, code-mining benchmarks, CI hardening."
  - id: future
    order: 8
    title: "FUTURE — Post-v1 Ideas"
    description: "Ideas for after the first public release."
# ============================================================
# OPEN ITEMS
# ============================================================
items:
  - key: BENCH-DOTNET-R5-IMPROVE
    summary: Raise .NET retrieval benchmark R@5 from 0.600 to 0.800
    type: task
    status: open
    priority: high
    size: M
    section_id: quality
    labels: [benchmark, dotnet, retrieval, ci]
    description: |-
      ## Problem

      The pinned CleanArchitecture .NET retrieval benchmark currently scores
      R@5=0.600 / R@10=0.850 on 20 known-answer queries. CI gates at the measured
      0.600 baseline so main stays green while still catching regressions, but
      the original quality target was R@5 >= 0.800.

      The weakest category is project_dependency (R@5=0.200 / R@10=0.600), where
      README and generated/client files often outrank `.csproj` files. Several
      misses are hit@10, which suggests reranking, metadata-aware query routing,
      or project-file-specific retrieval could lift R@5 without changing the
      corpus.

      ## Scope

      - Analyze the 8 current R@5 misses from `benchmarks/dotnet_bench.py`.
      - Improve retrieval for project files and architecture-oriented .NET
        queries without hard-coding benchmark expected filenames.
      - Keep the benchmark pinned to
        `jasontaylordev/CleanArchitecture@5a600ab8749c110384bc3bd436b9c67f3067b489`.
      - Raise `.github/workflows/dotnet-bench.yml` `R5_THRESHOLD` only after the
        measured benchmark reaches the new threshold.

      ## Acceptance Criteria

      1. `python benchmarks/dotnet_bench.py --repo-dir /tmp/CleanArchitecture`
         reports R@5 >= 0.800 on the pinned corpus.
      2. Project dependency category improves above the current R@5=0.200.
      3. `.github/workflows/dotnet-bench.yml` raises `R5_THRESHOLD` to the new
         measured baseline.
      4. `benchmarks/README.md` records the new baseline and reproduction output.
      5. Ruff and relevant tests pass.

  - key: MCP-REQUEST-COMPAT-UPSTREAM
    summary: Harden MCP request handling using upstream compatibility fixes as examples
    type: bug
    status: open
    priority: high
    size: S
    section_id: mcp_developer_tools
    labels: [mcp, compatibility, upstream, hardening]
    description: |-
      ## Symptom

      Upstream vanilla MemPalace fixed several MCP protocol compatibility edges
      after this fork split. Our state is too different for a direct port, but
      the behavior is relevant to `mempalace-code`:

      - `handle_request()` crashes when a client sends `tools/call` with
        `"arguments": null`.
      - Extra client arguments such as Gemini-style `wait_for_previous` reach
        handlers and produce internal tool errors.
      - Unknown `notifications/*` methods return errors instead of being ignored
        as notifications.

      These were reproduced locally against `mempalace_code.mcp_server`.

      ## Steps to reproduce

      Run a tiny Python snippet that calls `handle_request()` with:

      1. `{"method": "tools/call", "params": {"name": "mempalace_status", "arguments": null}}`
      2. `{"method": "tools/call", "params": {"name": "mempalace_status", "arguments": {"wait_for_previous": false}}}`
      3. `{"method": "notifications/cancelled", "params": {}}`

      Current behavior: case 1 raises `AttributeError`, case 2 returns an
      internal tool error, and case 3 returns an unknown-method response.

      ## Scope

      - Treat missing/null tool arguments as `{}` before type coercion.
      - Drop known client compatibility noise arguments that are not in the
        selected tool schema.
      - Return no response for `notifications/*` methods.
      - Keep the current `mempalace-code` server/tool names unchanged.

      ## Acceptance Criteria

      1. `tools/call` with `"arguments": null` succeeds for an argument-free
         tool such as `mempalace_status`.
      2. `tools/call` with `wait_for_previous` on a normal tool does not produce
         an internal tool error.
      3. Unknown `notifications/*` methods return `None` / no JSON-RPC response.
      4. Existing MCP request tests still pass.
      5. Ruff clean.

  - key: MINE-SKIP-GENERATED-ENTITIES
    summary: Skip generated entities.json during project mining
    type: bug
    status: open
    priority: high
    size: XS
    section_id: coding_first
    labels: [miner, init, upstream, hygiene]
    description: |-
      ## Symptom

      Upstream vanilla MemPalace now skips generated `entities.json` during
      mining. Our fork already skips `mempalace.yaml`, `mempal.yml`, and
      `.gitignore`, but `entities.json` is still mined as project content.

      ## Steps to reproduce

      Create a temporary project directory containing:

      - `mempalace.yaml`
      - `entities.json`
      - `notes.txt`

      Then call `scan_project(project_dir, respect_gitignore=False)`.

      Current behavior: the returned file list includes `entities.json`.

      ## Scope

      - Add `entities.json` to the generated/config file skip path.
      - Preserve `include_ignored` / force-include behavior for users who
        explicitly ask to mine that file.
      - Do not change entity detection output or `init` behavior.

      ## Acceptance Criteria

      1. `scan_project()` excludes `entities.json` by default.
      2. Explicit force-include can still include `entities.json`.
      3. Existing `mempalace.yaml` / `mempal.yml` skip behavior is unchanged.
      4. Focused miner tests pass.
      5. Ruff clean.

  - key: READ-PATH-NONE-METADATA-HARDEN
    summary: Tolerate None documents and metadata on search and graph read paths
    type: bug
    status: open
    priority: high
    size: S
    section_id: storage_reliability
    labels: [search, graph, robustness, upstream, migration]
    description: |-
      ## Symptom

      Upstream hardened read paths for palaces that contain rows with missing
      documents or metadata. LanceDB should normally preserve metadata, but
      `mempalace-code` can still see bad rows via legacy Chroma imports,
      partial/corrupt data, or direct store manipulation.

      ## Steps to reproduce

      Use small fake stores or monkeypatches that return valid result rows mixed
      with `None` metadata/documents:

      - `search_memories()` crashes when one result metadata entry is `None`.
      - `palace_graph.build_graph()` crashes when one batch metadata entry is
        `None`.

      ## Scope

      - Treat `None` metadata as an empty dict on search result rendering.
      - Treat `None` documents as empty text where ranking/rendering needs a
        string.
      - Skip `None` metadata rows in `build_graph()` rather than failing the
        entire graph/tunnel request.
      - Add small tests with fake stores; do not require corrupt LanceDB fixtures.

      ## Acceptance Criteria

      1. `search_memories()` returns sentinel metadata values instead of raising
         on `None` metadata.
      2. `code_search()` handles `None` metadata/doc rows without raising.
      3. `palace_graph.build_graph()` skips `None` metadata rows.
      4. Relevant search and graph tests pass.
      5. Ruff clean.

  - key: CONVO-NORMALIZE-GEMINI-CLAUDE-TOOLS
    summary: Extend conversation normalization with Gemini CLI and Claude Code tool-block cleanup
    type: feature
    status: open
    priority: medium
    size: M
    section_id: cleanup
    labels: [conversation, normalize, gemini, claude-code, upstream]
    description: |-
      ## Problem

      Upstream vanilla MemPalace added Gemini CLI JSONL support and improved
      Claude Code JSONL cleanup/tool-block formatting. Our normalizer supports
      Claude.ai JSON, ChatGPT JSON, Claude Code JSONL, Codex JSONL, Slack JSON,
      and plain text, but not Gemini CLI JSONL.

      Direct port is not appropriate because this fork keeps different package
      names, spellcheck behavior, and conversation-mining priorities. Use
      upstream as an example only.

      ## Scope

      - Add a local Gemini CLI JSONL parser for the current Gemini CLI transcript
        shape.
      - Preserve existing Codex JSONL and spellcheck behavior.
      - Improve Claude Code JSONL handling for tool_use/tool_result blocks and
        line-anchored system/hook noise, without deleting user prose.
      - Update module docs/tests for supported formats.

      ## Acceptance Criteria

      1. Gemini CLI JSONL fixture normalizes into `> user` / assistant-style
         transcript text.
      2. Claude Code tool_use/tool_result blocks are represented compactly and
         deterministically.
      3. Noise stripping is line/tag anchored and cannot eat neighboring user
         content.
      4. Existing normalize tests still pass.
      5. Ruff clean.

  - key: SEARCH-HYBRID-RERANK-EVAL
    summary: Evaluate BM25 plus vector reranking for code and project-file retrieval
    type: task
    status: open
    priority: medium
    size: M
    section_id: quality
    labels: [search, benchmark, retrieval, upstream, bm25]
    description: |-
      ## Problem

      Upstream vanilla MemPalace added BM25 + vector hybrid reranking. Our fork's
      current .NET benchmark misses include exact-ish project dependency queries
      where README/generated files outrank `.csproj` project files. BM25 may help,
      but this fork has LanceDB, code metadata, and benchmark constraints that
      differ from upstream, so the upstream implementation should be treated as
      an example, not a direct port.

      This item is separate from the currently-running
      `BENCH-DOTNET-R5-IMPROVE`; close or narrow it if that task already lands an
      equivalent measured solution.

      ## Scope

      - Prototype BM25 or metadata-aware reranking on top of current LanceDB
        candidate retrieval.
      - Measure impact on `benchmarks/dotnet_bench.py`, especially the
        `project_dependency` category.
      - Avoid hard-coding expected benchmark filenames.
      - Decide whether the reranker belongs in general search, `code_search`,
        benchmark-only experiment code, or not at all.

      ## Acceptance Criteria

      1. Benchmark before/after output is recorded.
      2. Any production reranker has focused tests for exact-token boosts and
         `None` document safety.
      3. Reranking does not regress existing search/code_search tests.
      4. If no production change is kept, document why and close the item with
         measured evidence.
      5. Ruff clean.

  - key: CODE-TREESITTER-GO-RUST-DETACH-TEST
    summary: Add negative tests for detached-comment gap detection in Go and Rust AST chunkers
    type: task
    status: open
    priority: low
    size: S
    section_id: coding_first
    labels: [testing, chunking, ast, go, rust]
    description: |-
      ## Problem

      _chunk_go_treesitter and _chunk_rust_treesitter use a blank-line gap check (b'\n\n' in source_bytes[prev.end_byte:next_sibling.start_byte]) to decide whether a leading comment/attribute is attached to the following declaration. The existing tests only verify the POSITIVE case (comment IS attached). There is no test asserting that a comment separated by a blank line is NOT absorbed into the following declaration's chunk.

      Without a negative test, removing the gap check would not break any existing assertion. This is the same gap identified in CODE-TREESITTER-PYTHON-DETACH-TEST for Python.

      ## Scope
      tests/test_chunking.py — add negative AST chunking tests for Go and Rust detached comments, with fixtures large enough to avoid adaptive merge masking the assertion.

      ## Deliverable

      Add two test functions to tests/test_chunking.py:
      1. test_ast_go_detached_comment_not_absorbed — uses GO_AST_COMMENT_ATTACHED fixture, asserts that the chunk containing 'func standalone' does NOT contain 'Detached comment'.
      2. test_ast_rust_detached_comment_not_absorbed — uses RUST_AST_ATTRIBUTE_ATTACHED fixture, asserts that the chunk containing 'fn default_config' does NOT contain 'Standalone comment'.

      Both tests require large enough function bodies that adaptive_merge_split does not merge them. May need fixture extensions (>=400 char bodies) similar to the approach planned for CODE-TREESITTER-PYTHON-DETACH-TEST.

      ## Acceptance Criteria

      1. test_ast_go_detached_comment_not_absorbed passes when tree-sitter-go is installed
      2. test_ast_rust_detached_comment_not_absorbed passes when tree-sitter-rust is installed
      3. Both tests skip when the respective grammar is absent
      4. Both tests would FAIL if the b'\n\n' gap check were removed from the respective chunker
      5. ruff clean

      Identified in CODE-TREESITTER-EXPAND harden round-1 (F-2).
    links:
      - type: is-blocked-by
        key: CLARIFY-CODE-TREESITTER-GO-RUST-DETACH-TEST
  - key: MINE-RUBY-SMART
    summary: Add smart symbol extraction for Ruby
    type: feature
    status: open
    priority: medium
    section_id: coding_first
    labels: [feat, miner]
    description: |-
      ## Problem
      Ruby is listed in miner but has no symbol extraction - only basic chunking.

      ## Scope
      - Upgrade existing .rb support
      - Extract: classes, modules, methods, attr_accessors, constants
      - Handle: metaprogramming patterns, DSLs, blocks

      ## Acceptance Criteria
      - [ ] Ruby added to _LANG_EXTRACT_MAP
      - [ ] Works on Rails projects
      - [ ] Tests for: classes, modules, methods, attr_accessor
    size: S
    links:
      - type: is-blocked-by
        key: CLARIFY-MINE-RUBY-SMART
  - key: MINE-HELM
    summary: Add Helm chart support to code miner
    type: feature
    status: open
    priority: medium
    section_id: coding_first
    labels: [feat, miner, infra]
    description: |-
      ## Problem
      Helm charts parsed as generic YAML without understanding template structure.

      ## Scope
      - Detect Chart.yaml, values.yaml, templates/
      - Extract: chart metadata, values schema, template resources
      - Handle: Go template syntax in YAML

      ## Acceptance Criteria
      - [ ] Helm charts indexed with chart name, version, dependencies
      - [ ] Template resources extracted despite {{ }} syntax
      - [ ] Tests for: Chart.yaml, values.yaml, templated Deployment
    size: S
    links:
      - type: is-blocked-by
        key: CLARIFY-MINE-HELM
  - key: MINE-ANSIBLE
    summary: Add Ansible support to code miner
    type: feature
    status: open
    priority: medium
    section_id: coding_first
    labels: [feat, miner, infra]
    description: |-
      ## Problem
      Ansible playbooks/roles parsed as generic YAML.

      ## Scope
      - Detect playbooks, roles, inventory
      - Extract: plays, tasks, handlers, variables, roles
      - Handle: Jinja2 templating in YAML

      ## Acceptance Criteria
      - [ ] Playbooks indexed by play name, hosts, tasks
      - [ ] Roles indexed by role name and structure
      - [ ] Tests for: playbook, role, inventory file
    size: S
    links:
      - type: is-blocked-by
        key: CLARIFY-MINE-ANSIBLE
  - key: CLARIFY-MINE-RUBY-SMART
    summary: "Clarify scope for MINE-RUBY-SMART before planning can proceed"
    type: task
    status: open
    priority: highest
    size: XS
    section_id: immediate
    assignee: owner
    description: |-
      ## Problem
      Clarification is required before MINE-RUBY-SMART can be planned.

      ## Scope
      Owner should clarify the expected Ruby support level, especially Rails DSL coverage, metaprogramming expectations, and the minimum symbol set required for a first pass.

      ## Acceptance criteria
      - Owner answers the scope questions or narrows MINE-RUBY-SMART to a first-pass deliverable.
      - CLARIFY-MINE-RUBY-SMART is marked done, or its is-blocked-by link is removed from MINE-RUBY-SMART.
      - Autopilot can re-run MINE-RUBY-SMART planning without timing out on ambiguous scope.
  - key: CLARIFY-MINE-HELM
    summary: "Clarify scope for MINE-HELM before planning can proceed"
    type: task
    status: open
    priority: highest
    size: XS
    section_id: immediate
    assignee: owner
    description: |-
      ## Problem
      Clarification is required before MINE-HELM can be planned.

      ## Scope
      Owner should clarify whether Helm support means chart metadata only, rendered-template-aware extraction, raw Go-template YAML parsing, or a smaller first-pass subset.

      ## Acceptance criteria
      - Owner defines the Helm first-pass scope and explicit out-of-scope behavior.
      - CLARIFY-MINE-HELM is marked done, or its is-blocked-by link is removed from MINE-HELM.
      - Autopilot can re-run MINE-HELM planning without timing out on ambiguous scope.
  - key: CLARIFY-MINE-ANSIBLE
    summary: "Clarify scope for MINE-ANSIBLE before planning can proceed"
    type: task
    status: open
    priority: highest
    size: XS
    section_id: immediate
    assignee: owner
    description: |-
      ## Problem
      Clarification is required before MINE-ANSIBLE can be planned.

      ## Scope
      Owner should clarify whether Ansible support covers playbooks only, roles and inventories, Jinja-templated YAML, or a smaller first-pass subset.

      ## Acceptance criteria
      - Owner defines the Ansible first-pass scope and explicit out-of-scope behavior.
      - CLARIFY-MINE-ANSIBLE is marked done, or its is-blocked-by link is removed from MINE-ANSIBLE.
      - Autopilot can re-run MINE-ANSIBLE planning without timing out on ambiguous scope.
  - key: CLARIFY-CODE-TREESITTER-GO-RUST-DETACH-TEST
    summary: "Clarify scope for CODE-TREESITTER-GO-RUST-DETACH-TEST before planning can proceed"
    type: task
    status: open
    priority: highest
    size: XS
    section_id: immediate
    assignee: owner
    description: |-
      ## Problem
      Clarification is required before CODE-TREESITTER-GO-RUST-DETACH-TEST can be planned.

      ## Scope
      Owner should confirm whether this task should only add regression tests, or may also adjust fixtures and chunking helper code if needed to make the tests meaningful.

      ## Acceptance criteria
      - Owner confirms test-only versus test-plus-fixture scope.
      - CLARIFY-CODE-TREESITTER-GO-RUST-DETACH-TEST is marked done, or its is-blocked-by link is removed from CODE-TREESITTER-GO-RUST-DETACH-TEST.
      - Autopilot can re-run CODE-TREESITTER-GO-RUST-DETACH-TEST planning without timing out on ambiguous scope.
