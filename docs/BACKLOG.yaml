# ============================================================
# BACKLOG.yaml — Machine-readable project backlog
# Format: sections + items (DH2-style)
# Parse:  backlog list --status open --file docs/BACKLOG.yaml
# ============================================================
#
# project: mempalace-code

sections:
  - id: immediate
    order: 1
    title: "IMMEDIATE — Foundation & Project Setup"
    description: "Fork identity, CI, docs. Must be done before any feature work."
  - id: pre_release
    order: 2
    title: "PRE-RELEASE — v1.0 Blockers (Autopilot Pass)"
    description: "P0 and P1 items that must land before a public v1.0. All items here are autopilot-ready — owner decisions already made. Batch in one large autopilot pass. Items needing owner judgment live in other sections."
  - id: coding_first
    order: 3
    title: "CODING-FIRST — Smart Code Mining"
    description: "Language-aware chunking, structural boundaries, code-optimized search. The core differentiator."
  - id: storage_reliability
    order: 4
    title: "STORAGE & RELIABILITY"
    description: "LanceDB hardening, migration tooling, backup/restore."
  - id: mcp_developer_tools
    order: 5
    title: "MCP — Developer-Focused Tools"
    description: "New and improved MCP tools for coding workflows."
  - id: cleanup
    order: 6
    title: "CLEANUP — De-emphasize Dialog-First Features"
    description: "Refactor or gate conversation-centric modules. Keep functional but not default."
  - id: quality
    order: 7
    title: "QUALITY — Tests, Benchmarks, CI"
    description: "Test coverage, code-mining benchmarks, CI hardening."
  - id: autopilot_demo
    order: 8
    title: "AUTOPILOT DEMO — Code Quality Showcase"
    description: "Measurable, public-safe improvements that demonstrate Autopilot can raise code quality without changing product scope."
  - id: future
    order: 9
    title: "FUTURE — Post-v1 Ideas"
    description: "Ideas for after the first public release."
# ============================================================
# OPEN ITEMS
# ============================================================
items:
  - key: AUTOPILOT-COMMIT-ATTRIBUTION-REPO-BOUNDARY
    summary: "Disable Autopilot commit attribution for this repository"
    type: bug
    status: open
    priority: highest
    size: XS
    section_id: immediate
    description: "## Symptom\n\n`autopilot config show --format json` resolves `commit_attribution.enabled=true` with a Claude Co-Authored-By trailer, while AGENTS.md forbids that trailer in every repository commit. Autopilot launch is unsafe until this is false.\n\n## Steps to reproduce\n\nFrom the exact v1.14.0 candidate checkout, run `autopilot config show --format json | jq .commit_attribution`.\n\n## Scope\n\nUse the existing Autopilot repository-config merge owner. Add only the smallest repo-local attribution override needed to disable trailers. Preserve the effective global profile and every phase model/effort route; add no model or profile override. This blocker may be repaired manually because Autopilot cannot safely commit its own enabling configuration.\n\n## Expected vs actual\n\nExpected: attribution disabled and no trailer instructions. Actual: default Claude co-author attribution is enabled.\n\n## Root cause hypothesis\n\nThe repository has no Autopilot config override, so the enabled default wins despite the public AGENTS.md boundary.\n\n## Acceptance criteria\n\n- AC-1: `autopilot config validate` passes and resolved `commit_attribution.enabled` is false.\n- AC-2: Resolved profile remains `operator-luna-sol` and the phase routing table is byte-equivalent before and after.\n- AC-3: No Claude Co-Authored-By trailer appears in new commits."
